{
  "name": "ecomapas-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy:functions": "firebase deploy --only functions",
    "deploy:web": "firebase deploy --only hosting",
    "deploy": "npm run deploy:functions && npm run deploy:web"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "firebase": "^5.4.0"
  }
}
